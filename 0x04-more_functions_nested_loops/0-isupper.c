#include "main.h"

/**
 * _isupper - checks for uppercase character
 * c returne 1 if is uppercase return 1 otherwise
 * Return: 1 if c is uppercase, 0 otherwise
 */
int _isupper(int c)
{
	return (c >= 'A' && c <= 'Z');
}
